# 🏫 School Submission System
**Версія:** 1.0.0

## 📝 Опис проекту
Це веб-платформа для автоматизації процесу збору, перевірки та оцінювання домашніх завдань учнів. Система забезпечує зручну взаємодію між вчителями та учнями, дозволяючи зберігати всі роботи в одному місці, вести історію змін та забезпечувати зворотній зв'язок.

Основна мета — замінити хаотичний обмін файлами через месенджери на структуровану систему з архівом, журналом оцінок та зручним переглядом файлів прямо у браузері.

## ✨ Основні можливості

### 👨‍🏫 Для Вчителя:
- **Панель керування (Dashboard):** Перегляд всіх зданих робіт з фільтрацією за класом, датою та пошуком по прізвищу.
- **Попередній перегляд файлів:** Вбудований переглядач для:
  - 📄 PDF документів
  - 🖼️ Зображень
  - 💻 Коду (з підсвіткою синтаксису)
  - 📊 Office документів (через конвертацію)
  - 📦 Архівів (перегляд вмісту)
- **Система оцінювання:** Виставлення оцінок (1-12 балів) з миттєвим збереженням.
- **Коментування:** Можливість залишати необмежену кількість коментарів до роботи, редагувати та видаляти їх.
- **Журнал активності:** Відстеження всіх дій (хто здав, хто оцінив, хто прокоментував).
- **Журнал оцінок:** Зведена таблиця успішності по класах.

### 👨‍🎓 Для Учня:
- **Проста здача робіт:** Завантаження файлів або посилань.
- **Стрічка успіху:** Перегляд статусу своїх робіт ("Оцінено"/"Не оцінено") без розголошення оцінок іншим.
- **Зворотній зв'язок:** Перегляд коментарів вчителя та оцінок у власному кабінеті.
- **Приватність:** Учні бачать лише власні деталі, загальна стрічка показує лише факт здачі.

## 🛠️ Технічна структура

```text
myproject2/
├── db.sqlite3              # База даних
├── manage.py               # Django менеджер
├── myproject2/             # Налаштування проекту
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── submissions/            # Основний додаток
│   ├── admin.py            # Адмінка
│   ├── models.py           # Моделі (Submission, Comment, ActivityLog)
│   ├── views.py            # Логіка (Views)
│   ├── urls.py             # Маршрутизація
│   ├── utils.py            # Утиліти (обробка файлів, конвертація)
│   ├── templates/          # HTML Шаблони
│   │   └── submissions/
│   │       ├── base.html
│   │       ├── file_viewer.html
│   │       ├── student_detail.html
│   │       ├── submission_detail.html
│   │       ├── submission_form.html
│   │       ├── success_feed.html
│   │       └── teacher_dashboard.html
│   └── static/             # Статичні файли (CSS, JS, Images)
└── test_site.py            # Скрипт автоматичного тестування
```

## 🚀 Встановлення та запуск

1. **Клонування репозиторію:**
   ```bash
   git clone <repository-url>
   cd new_projectsite
   ```

2. **Створення віртуального середовища:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/macOS
   # venv\Scripts\activate  # Windows
   ```

3. **Встановлення залежностей:**
   ```bash
   pip install django colorama
   ```

4. **Міграція бази даних:**
   ```bash
   python manage.py migrate
   ```

5. **Створення суперкористувача (Вчителя):**
   ```bash
   python manage.py createsuperuser
   ```

6. **Запуск сервера:**
   ```bash
   python manage.py runserver
   ```

## 🧪 Тестування
Проект містить вбудований скрипт для перевірки працездатності всіх вузлів:
```bash
python test_site.py
```

## 🔒 Безпека та Права
- **Вчителі:** Мають повний доступ до всіх робіт, оцінювання та коментарів.
- **Учні:** Мають доступ лише до форми здачі та перегляду власних результатів.
- **Захист:** Використовується CSRF захист, перевірка прав доступу (@login_required) та валідація вхідних даних.

---
*Розроблено для покращення освітнього процесу. Опис може бути не точним, так як створений за допомогою ШІ*
